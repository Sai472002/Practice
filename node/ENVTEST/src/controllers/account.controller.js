const accmodel = require("../models/account.model")

const accController = async(req,res) =>{
    try {
        console.log("test",req.body);
        
    } catch (error) {
        
    }
}

